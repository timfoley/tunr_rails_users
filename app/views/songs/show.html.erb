<h2><%= @song.title %> <%= link_to("(edit)", edit_song_path(@song)) if @current_user %></h2>
<h3>By: <%= link_to @song.artist.name, @song.artist %></h3>

Album: <%= @song.album %></br>

<audio controls src="<%= @song.preview_url %>"></audio>
